using System.Collections;
using System.Collections.Generic;
using System.Text;

using UnityEngine;

public class EnigmaCode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    [ContextMenu("Run Code")]
    public string Encrypt()
    {
        /*string codeString = "!";
        foreach (char c in text)
        {
            int hex = c;
            int hex2 = hex + 13;
            if(hex2 > 90)
            {
                hex2 = (hex2 - 26);
            }

            codeString += System.Convert.ToChar(hex2);
        }*/
        return " fail";
       
    }
}
